# 任务进度查询契约

升级服务已有 GET /api/v1/upgrade/tasks/:id，用于返回任务状态、步骤、进度百分比、当前步骤和预计剩余时间。

GAIOP-Admin 不直接暴露此服务；Admin BFF 使用内部令牌查询，再过滤内部字段供页面展示。进度查询是只读接口，不能执行任务、修改状态或替代升级服务的审计。

页面只在任务运行或自动回滚期间以短间隔查询，终态停止。部署阶段应确保升级服务仅在 ISO 内部网络监听。
