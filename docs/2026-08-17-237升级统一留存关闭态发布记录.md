# 2026-08-17 237 Upgrade 统一留存关闭态发布记录

## 发布范围

第十批先发布 Upgrade 第九批收口版本，包含第一批升级包/staging/回滚备份保护和 Upgrade SQLite 日、周、月备份实现。SQLite 备份目录与组件升级回滚目录严格分开；创建和清理开关保持关闭。本批未部署后端、未应用系统日志模板、未调用清理器、未删除生产数据。

## 实时发布证据

- 发布批次：`20260817T083414Z`
- 生产目录：`/opt/gaiop-upgrade-e2e-20260805`
- 数据库：`/var/lib/gaiop-upgrade/napm-upgrade.db`
- 回滚点：`/var/backups/gaiop/deployments/upgrade-retention-20260817T083414Z`
- 发布包 SHA-256：`5041715bf6ba4fe06e347cd8e58f243b9c7817feb5bcb7fe4204c4cf32efcc12`
- SQLite 在线备份完整性：`PRAGMA integrity_check=ok`

数据库总行数为 `19`，其中升级任务 `2`、组件 `14`、备份 `0`；发布前后不变。受管目录计数发布前后不变：升级包 `2`、staging `0`、组件回滚目录 `3`。第一批 Upgrade 清理 one-shot 返回关闭态，SQLite 备份 one-shot 返回 `create_disabled`，SQLite 备份 timer 为 `inactive/disabled`。

## 服务与关闭态

Upgrade 健康接口返回 `200`，未认证接口返回 `401`，监听保持 `127.0.0.1:18900`。`gaiop-upgrade.service` 为 `active`。Upgrade 清理 timer 发布后已停止并禁用；不跨入组件回滚目录，不改变最近可用回滚版本保护规则。Admin 发布在 Upgrade 健康验证完成后进行。

磁盘水位监测属于 Admin 配置，Upgrade 仅作为 `upgrade_state` 和 `upgrade_rollback` 的受管路径来源；本批水位 timer 因 `upgrade_rollback` 无法由 `gaiop` 完成 `stat/statfs` 而保持禁用。

## 未实施

本批没有启用任何清理、备份创建、报告/会话处理或水位 timer，没有读取或删除生产会话、附件和业务文件。后端留存集成、80%/90%治理、高容量写入阻断和日志配置均未部署或应用。

## 测试

Node 22.23.2 下全仓测试 `89/89` 通过，JavaScript 语法与 `git diff --check` 通过。Node 24 直接运行会因本机 `better-sqlite3` 的 ABI 127/137 不匹配失败，属于运行时不匹配，不是代码回归。
