# 2026-08-05 报告归因适配层升级保护发布记录

## 目的

报告来源登记已从报告 Skill 和后端插件中解耦为独立 Sidecar。为避免后续升级覆盖 Admin 或报告后端后留下“生成正常、报告管理器不再登记来源”的静默故障，升级流程在每个升级器自身冒烟测试通过后增加 Sidecar 索引保护。

## 契约

- 生产开启 `GAIOP_REPORT_ATTRIBUTION_REQUIRED=true`；
- 索引固定为 `/var/lib/gaiop/report-attribution/index.json`；
- 必须符合 `gaiop.report-attribution.v1`，`entries` 必须为数组；
- `updatedAt` 最大允许滞后 30 秒，未来时间偏差不超过 5 秒；
- 检查失败会使 `smoke_test` 失败，并复用原升级引擎的自动回滚，不绕过任何既有升级器冒烟测试。

## 237 发布结果

- 代码提交：`8af8225`；
- `gaiop-upgrade.service`：`active`；
- 健康检查：200；未鉴权业务接口：401；
- 监听：仅一个回环 `18900`；
- 归因保护：已开启，发布验证读取到 17 条新鲜索引；
- 回滚备份：`/var/backups/gaiop/upgrade-guard-20260805T053612Z`；
- Admin、Gateway 和 Sidecar 在发布后均为 `active`。

全量历史测试在已部署基线与候选代码上均复现同一组 13 个夹具失败；部署门禁确认失败套件集合一致，并单独要求新增保护测试 3/3 通过。该历史测试债务不属于本次保护逻辑，但后续应单独修复。

本记录是 2026-08-05 的发布快照；后续操作仍须重新核查 Git、服务、索引新鲜度和监听。
